interface:
  display_name: "Efficient Blender Modeling"
  short_description: "Build and verify Blender assets with low context"
  default_prompt: "Use $blender-efficient-modeling to create and verify this Blender asset efficiently."

policy:
  allow_implicit_invocation: true
